import { WebapiLoginCredential } from './webapi-login-credential';

describe('WebapiLogin', () => {
  it('should create an instance', () => {
    expect(new WebapiLoginCredential()).toBeTruthy();
  });
});
